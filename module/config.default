# C700 KeyMap 默认配置
#
# 左边是源按键：F10 自定义键、F11 左背键、F12 右背键
# 右边是目标按键，可以写名称或者数字；末尾加 ! 开启连发，! 后面可以跟频率
# 在应用里改过配置之后，这份文件不会再覆盖你的设置
#
# 同一个按键不要在厂商快设里重复映射，否则会触发两次

F10=SYSRQ
F11=BUTTON_THUMBL
F12=BUTTON_THUMBR
